# ClassLoop 项目交接与进度说明

> 更新日期：2026-09-03。面向第一次接触项目的队友。下次阶段演示：2026-09-07（周一）。
> 本文区分“代码已实现”“已做过验证”和“未来设计”，不把规划、按钮占位或预置数据当成完成成果。
> 本次仅整理交接文档、核对真实课件目录；没有批量解析课件、启动服务或开发新的业务功能。

## 1. 先读这一页：项目是什么、现在到哪了？

| 问题 | 交接结论 |
|---|---|
| 项目定位 | 面向同步理论课堂的认知反馈应用：将学生作答和疑问关联到课堂、课件页及知识点，辅助教师发现理解障碍、调整讲解、进行复测。 |
| 示例学科 | 数据结构；此前演示围绕 BST / AVL / 树高与查找复杂度。现在已加入真实课程资料，可据此选择教学片段。 |
| 目标用户 | 教师：组织课堂、判断理解情况；学生：低门槛参与、私下反馈；管理员：只读查看业务数据与图谱。 |
| 已形成的流程 | 三入口 → 教师注册/建课堂/建题 → 学生临时加入/作答 → 教师实时统计/广播 → 管理员查看 SQLite 与 Neo4j。 |
| 课件链路 | PPTX/PDF → 本地逐页提取 → 文本分块/规则关键词 → SQLite 保存 → Neo4j 同步 → 管理员图谱展示，已经实现。 |
| 最大缺口 | 教师网页上传课件、教师切页同步、学生页级私下反馈、真实 AI 诊断、干预后复测尚未打通。 |
| 是否接入大模型 | 当前 ClassLoop 运行链路没有实际调用大模型或 VentureAgent；现有误区面板是规则演示。 |
| 阶段完成度如何表述 | 可支撑约 40%—50% 的 MVP 阶段汇报，这是工程判断而非正式评分。不要声称已完成完整智能教学闭环。 |
| 原创性边界 | 前端复用了 PollUP 的部分界面/交互基础；ClassLoop 的业务数据层、三入口隔离、实时事件与课件图谱是本项目实现。不能称全部从零原创。 |
| 当前启动状态 | 上一次检查时前后端与 Neo4j 均未连接。进程可能随会话关闭，接手后应按第 7 节重新启动；源码和持久化数据不等于正在运行的服务。 |

### 1.1 最终设计与当前状态

| 目标环节 | 当前状态 | 面向用户的价值 |
|---|---|---|
| 课前：上传课件、绑定课堂 | 后端已做，教师上传界面待做 | 从真实教材建立教学上下文 |
| 课中：老师翻到某页，学生同步 | 待开发 | 对齐师生正在讨论的内容 |
| 课中：学生反馈当前页疑问 | 待开发 | 减少公开举手压力，定位具体障碍 |
| 课中：理解检查与实时统计 | 已实现基础版 | 让教师看到班级回答分布 |
| AI：聚合误区，关联页面/知识点 | 规则演示；语义诊断待开发 | 将零散回答转成有证据的教学建议 |
| 教师：选择解释、反例或复测 | 普通广播已实现；智能干预待开发 | 由教师控制教学决策 |
| 课后：初测/复测对比 | 待开发 | 用真实作答变化验证教学效果 |

定位表述建议：**不是单纯生成教案或回答问题，而是围绕“当前讲到哪里、学生哪里不懂、教师如何响应、响应后是否改善”组织课堂数据。** 这属于待验证的产品差异化，尚无竞品调研或教学实验支撑“市场唯一”等结论。

## 2. 技术栈与工程边界

| 层级 | 当前采用 | 职责与边界 |
|---|---|---|
| 前端 | React 19、TypeScript、Vite 6、Tailwind CSS 3 | 三端页面、表单、统计展示、SVG 图谱；路由主要在 App.tsx 中按 pathname 分发 |
| 前端数据层 | frontend/src/services/dataProvider.tsx | 统一 REST 请求、登录令牌、查询刷新、SSE 事件；名称仍保留部分 elements/sessions 风格 |
| 后端 | Python、FastAPI、Uvicorn | 身份认证、课堂/题目/回答 API、文件与文档处理、管理员只读 API |
| 关系数据库 | SQLite，Python sqlite3 直接访问 | 业务事实主存储；启用外键和 WAL；当前不是 SQLAlchemy ORM 实现 |
| 图数据库 | 独立 Neo4j Docker 容器、Python Neo4j Driver | ClassLoop 课件结构与关键词关系，不保存登录账号 |
| 文档解析 | python-pptx、pypdf | 提取 PPTX 文本/表格和 PDF 可提取文本；不包含 OCR 或多模态理解 |
| 实时机制 | HTTP 提交 + SSE 推送 + 10 秒轮询补偿 | 学生作答/加入/退出、教师广播；不是已经部署的 WebSocket 集群 |
| 图谱前端 | 自有 SVG 分列布局 | 课件概览、逐页子图、节点点击查看文本；不是嵌入 Neo4j Desktop |
| 外部 AI 工具 | VentureAgent：只读、尚未接入当前业务 | 后续只能通过 ClassLoop 自己的 HTTP Adapter 调用；不得修改、import 或复制外部服务源码 |
| 参考仓库 | ClassLoop/PollUP | 只读参考，不是当前应用启动目录；当前 ClassLoop 不需要 Convex 服务 |

本机本次核对版本：Python **3.12.13**、Node.js **24.13.1**。这只是已用环境记录，不代表只能使用这两个精确版本；接手机器应按依赖清单安装并重新验证。

```text
学生 / 教师 / 管理员浏览器
              │ REST + SSE
              ▼
       ClassLoop FastAPI :8100
         ├─ SQLite：业务事实、文件、提取结果
         ├─ Neo4j :7688：课件结构与关键词关系
         └─ 内存：临时昵称/头像、在线名单、SSE 订阅

未来：FastAPI → 脱敏课堂上下文 → VentureAgent/模型 API → 证据化建议
```

## 3. 三端功能清单

状态口径：“已实现”指存在可用代码及对应路径；不代表已通过生产安全审计或大规模压力测试。

### 3.1 公共入口与学生端

| 功能 | 进度 | 数据/操作说明与限制 |
|---|---|---|
| 三入口主页、学生快捷加入 | 已实现 | 主页不检查教师/管理员账号；进入对应入口才处理身份 |
| 6 位课堂码校验 | 已实现 | 查询 SQLite；不存在时中文提示，支持重新输入/查询，不应无限转圈 |
| 课堂开放状态检查 | 已实现 | 暂停课堂禁止加入/提交回答 |
| 临时头像与用户名 | 已实现 | 选择预设动物/表情头像、输入昵称；没有学生账号注册 |
| 退出与离线清理 | 已实现 | 主动退出清理在线资料；10 分钟无心跳后按清理逻辑过期；后端重启也丢失临时资料 |
| 单选、多选 | 已实现 | 每个学生独立选择，可多人选择同一选项；不再抢占选项 |
| 文本、数值滑块、文件回答 | 已实现 | 回答与文件保存到 SQLite；普通文件上传接口限制 1.5 MB |
| 条件题显示 | 已实现基础版 | 后端查询前置回答，按选项、文本、数值条件决定可见题目 |
| 已答题读取和更新 | 已实现 | 同一个临时 participantId 对同一题更新原回答；刷新/重新进入可能生成新身份，不是长期学习档案 |
| 作答进度、前后题、完成页 | 已实现 | 完成页使用教师设置；具体题型有不同输入交互 |
| 接收教师提示 | 已实现 | SSE 即时提示，并可读取数据库中的广播历史；当前不是师生私聊 |
| 查看结果页 | 已实现基础版 | 公开结果/前端 PIN 门禁；后端权限存在缺口，见第 9 节 |
| 跟随教师课件页、页级疑问按钮 | 未实现 | 下周一前优先目标 |
| AI 辅导、个性化解释、长期历史 | 未实现 | 不要将演示面板称为已接通的 AI 导师 |

### 3.2 教师端

| 功能 | 进度 | 数据/操作说明与限制 |
|---|---|---|
| 注册、登录、退出、恢复登录 | 已实现 | 邮箱不区分大小写唯一；密码保存哈希；令牌存在浏览器和服务端会话表 |
| 教师/管理员登录环境隔离 | 已实现 | 分别使用浏览器令牌键，不互相覆盖；学生不使用这两类令牌 |
| 课堂列表、创建、编辑 | 已实现 | 标题、描述、课堂码、教师归属持久化 |
| 暂停/开放、复制、删除课堂 | 已实现 | 删除课堂会级联删除相关题目和回答；演示时不要误删正式数据 |
| 学生/结果链接、二维码下载 | 已实现 | 由当前网站地址生成；默认 localhost 链接不能供其他设备访问 |
| 结果公开/PIN 设置 | 已实现但须加固 | 配置已入库，前端会校验；不能视为可靠的后端访问控制 |
| 完成提示、图片、背景/强调色 | 已实现 | 配置与关联图片持久化 |
| 题目增删改、复制、上下排序 | 已实现 | 题干、选项、图片、正确选项标记、启用状态与顺序保存到数据库 |
| 条件逻辑、数值范围设置 | 已实现基础版 | 需补齐边界输入与服务端答题规则校验 |
| 题目 JSON 导入/导出、选项 CSV 导入 | 已实现 | 不是自动从 PPT 生成题目；外部文件仍需格式校验 |
| 实时回答分布、人数、文本/文件结果 | 已实现基础版 | 回答从 SQLite 读取，部分汇总在浏览器计算；非模型生成的统计 |
| 在线参与者与已答学生列表 | 已实现 | 在线时可见临时昵称/头像；离线后回答仍在，可能显示匿名学生 |
| CSV 导出、清空回答、删除单个参与者回答 | 已实现 | 删除回答不等于封禁或踢出学生 |
| 全班文字广播 | 已实现 | 最多 280 字；持久化并通过 SSE 通知 |
| 课堂计时器 | 已有页面/组件 | /timer 支持计时交互和全屏等；不是数据库同步计时，更不是课件页同步 |
| 误区面板 | 部分实现 | 数据来自作答，但主题/知识链仍固定为 BST 示例；干预按钮有占位提示 |
| PPTX/PDF 上传、提取、同步、重试、删除 | 后端已实现 | 教师账号可通过 API 使用，尚无完整教师网页课件管理界面 |
| 教师切页、页级反馈、智能干预与复测 | 未实现 | 需在已有文档和事件链路上继续开发 |

### 3.3 管理员端

| 功能 | 进度 | 实际展示范围 |
|---|---|---|
| 独立登录、管理员角色校验 | 已实现 | /admin；后端管理员接口检查角色 |
| 双数据库一级标签 | 已实现 | “关系数据库”和“Neo4j 知识图谱” |
| 业务统计卡片 | 已实现 | 教师、课堂、题目、回答、课件、临时在线人数；在线人数来自内存 |
| 教师表 | 已实现 | 姓名、邮箱、课堂数、创建时间；不显示密码哈希 |
| 课堂表 | 已实现 | 标题、课堂码、教师、题目数、回答数、开放状态 |
| 题目表 | 已实现 | 题目、课堂、题型、回答数、启用状态 |
| Neo4j 连接状态、全部教师课件列表 | 已实现 | 列表来自 SQLite，节点/关系直接查询 Neo4j |
| 课件概览与逐页子图 | 已实现 | 概览展示课件/页面；选页展示该页文本块与关键词，点击节点查看文本 |
| 数据库通用表编辑器 | 未实现，也非当前目标 | 目前是受控业务只读视图，不是任意 SQL/Cypher 控制台 |
| 回答明细、全部文件、图谱搜索/缩放/拖动 | 未实现或待增强 | 当前图谱采用固定分列布局与滚动展示 |
| 账号管理、重置密码、删除、审计、备份面板 | 未实现 | 不要向队友承诺完整运维后台 |

## 4. 大模型与“智能功能”进度

| 能力 | 是否调用大模型 | 当前进度 | 下一步与验收证据 |
|---|---|---|---|
| PPTX/PDF 文本提取 | 否 | 已实现 | 真实课件逐页抽样核对页码和中文文本 |
| 文本分块、关键词抽取 | 否 | 已实现规则版 | 关键词可能是标题或短句，不是经过模型确认的学科概念 |
| Neo4j 构图 | 否 | 已实现结构图谱 | 包含/顺序/提及关系；不能称为完整课程先修知识推理 |
| 作答统计、人数/百分比 | 否 | 已实现基础版 | 应由程序计算，未来也不交给模型编造 |
| 误区诊断 | 否 | BST 规则演示 | 先去掉写死主题；再关联真实题目与回答证据 |
| VentureAgent 调用 | 计划使用 | 当前业务未接入 | 旧迁移文档的接口考察不等于实际集成；需要 Adapter、超时和响应校验 |
| 学生疑问语义聚类 | 计划使用 | 未实现 | 将相似问题归并到具体页面/知识点，保留原始证据 |
| 解释、反例、教学建议 | 计划使用 | 未实现 | 输入脱敏汇总和页码，输出结构化建议，教师确认后发布 |
| 自动出题、变式复测 | 计划使用 | 未实现 | 模型生成候选题；教师审核；初测复测关联与变化指标由程序计算 |
| OCR、图片/公式理解、向量检索/RAG | 可选 | 未实现 | 不作为周一必须项；纯图片 PDF 当前可能标记 needs_ocr |

未来调用路径：`ClassLoop 后端 → 当前页/知识点 + 脱敏回答汇总 → VentureAgent 或平台模型 → 结构化建议 + 来源证据 → 教师确认`。密钥放服务端，不进入前端，不发送学生临时姓名/头像；统计事实、建议文本与模拟数据须分开标注。

## 5. 数据库：保存了什么、用来判断什么？

| 存储 | 表/对象 | 已承载的判断或查询 |
|---|---|---|
| SQLite | users、auth_sessions | 邮箱唯一、密码校验、令牌有效期、账号角色 |
| SQLite | class_sessions | 课堂码查找、教师归属、开放状态、结果配置 |
| SQLite | questions | 题目顺序/状态、选项、条件逻辑与题目内容 |
| SQLite | student_responses | 作答查询和更新、统计；唯一键是“参与者 + 题目”，不是“选项只能被一人选” |
| SQLite | files | 图片、学生附件、课件原文件二进制 |
| SQLite | classroom_events | 教师广播历史；不是所有 SSE 通知都持久化为事件记录 |
| SQLite | learning_documents、document_pages、document_chunks | 课件归属/绑定、原文、页码、切块、图谱 JSON、同步状态 |
| SQLite | courses、lessons | 已有表与演示数据；课程/课次管理尚未形成前端完整业务 |
| Neo4j | ClassLoopDocument / Session / Page / Chunk / Keyword | 课件结构与关键词查询；使用独立 ClassLoop 标签和 namespace=classloop |
| Neo4j | USES_DOCUMENT / HAS_PAGE / NEXT_PAGE / HAS_CHUNK / NEXT_CHUNK / MENTIONS | 课堂绑定、层级、顺序和文本提及；未绑定课堂的课件没有 Session 关系 |
| 后端内存 | 在线名单、SSE 订阅 | 临时昵称/头像、心跳、即时通知；重启后不保留 |
| 浏览器 | 教师/管理员登录令牌；学生页面状态 | 教师和管理员分别保存令牌；学生临时身份不构成长期账号 |

| 持久化位置/状态 | 说明 |
|---|---|
| backend/data/classloop.db | 默认业务数据库；由后端自动建表，不需要安装 SQLite 服务 |
| classloop.db-wal / classloop.db-shm | SQLite 运行期文件；不要随意删除。备份应使用一致性备份或停止写入后操作 |
| Neo4j Docker 卷 | 按当前 Compose 默认项目名通常为 backend_classloop_neo4j_data / logs；以 docker volume ls 为准 |
| 未配置 Neo4j | 提取结果仍保存，graphStatus=pending |
| 已配置但连接/同步失败 | 通常为 failed，保留 SQLite 数据；通过同步接口重试，没有自动任务队列 |
| 已同步 | graphStatus=synced；管理员图谱查询仍要求 Neo4j 当前可用 |
| 最近已验证样例 | 项目介绍 PPT：8 页，77 节点，136 关系；不是新加入的真实课程 PDF，且未绑定课堂 |
| 最近业务快照 | 3 教师、2 课堂、4 题、20 回答、1 课件；包含预置/测试数据，后续会变化，不是实际教学效果 |

## 6. 真实课件资料与使用建议

目录：`ClassLoop/数据结构课件/`，本次核对共 **18 份 PDF + 1 份 DOCX**。这里只核对了文件名，尚未逐页阅读、解析或导入数据库。

| 资料分组 | 文件 | 建议用途 |
|---|---|---|
| 数据结构基础 | 1 绪论-80.pdf；2 线性表-80.pdf；3 栈和队列-2024.pdf；4 数组和广义表-2024.pdf | 备用课堂场景 |
| 树与查找 | 5.树和二叉树-2024.pdf；7.查找-2024.pdf | 优先人工确认是否含 BST/AVL 对应内容，再选连续小节作为周一样例 |
| 图与排序 | 6.图-2024.pdf；8 排序-2024.pdf；8.7-外部排序-2024.pdf | 后续拓展主题 |
| 算法设计 | 算法-1 树的遍历与回溯法-2024.pdf；算法-2 贪心算法-2024.pdf；算法-3 分治策略-2024.pdf；算法-4 动态规划-2024.pdf | 后续算法理解与误区场景 |
| 计算理论 | 计算理论-1-计算模型-图灵机.pdf；计算理论-1-计算模型-自动机- 2024.pdf；计算理论-2-可计算理论 - 2024.pdf；计算理论-3-计算复杂性 - 2024.pdf | 后续理论教学场景 |
| 其他 | 数据结构总复习2024年计算理论 精简(1).pdf；数据结构.docx | 复习资料；DOCX 当前不支持课件 ingest |

周一只选一小节，人工确定 5—8 页、3 道理解题和一个常见误区。不要直接对全部资料调用模型。保留源文件；如制作节选，另存副本并记录原始页码映射；课程资料对外发布需确认许可。

## 7. 从零启动：需要开哪些软件？

### 7.1 服务开关表

| 软件/服务 | 普通课堂业务演示 | 包含图谱的完整演示 | 说明 |
|---|---|---|---|
| VS Code | 推荐 | 推荐 | 打开整个 ClassLoop 目录，不是 PollUP，不是在根目录直接 npm run dev |
| Python / FastAPI 后端 | 必须 | 必须 | 端口 8100；终端保持运行 |
| Node.js / Vite 前端 | 必须 | 必须 | 端口 5173；终端保持运行 |
| SQLite | 自动 | 自动 | 后端直接读文件，不需要 Desktop、数据库服务器或单独启动命令 |
| Docker Desktop | 不要求 | 必须（当前部署方式） | 等待 Docker Engine running；一般正常启动即可，权限错误再检查管理员权限 |
| ClassLoop Neo4j 容器 | 不要求 | 必须 | 启动 Docker Desktop 不等于容器一定已经运行，要用 compose 检查 |
| Neo4j Desktop | 不需要 | 不需要 | 当前采用 Docker 容器；不要同时再建一套 Desktop 数据库 |
| Neo4j Browser 网页 | 不需要 | 可选 | 外部检查工具；管理员内置图谱页已能直接展示 |
| VentureAgent | 不需要 | 不需要 | 当前没有接入，启动它不会自动获得 AI 功能 |
| Convex / PollUP 原版服务 | 不需要 | 不需要 | 不属于当前应用运行链路 |

### 7.2 打开正确目录

```powershell
cd "C:\Users\86185\Desktop\2026-2027\大数据技术课程实践\ClassLoop"
code .
```

如果 code 命令不可用，在 VS Code 选择“文件 → 打开文件夹”，选择上述 ClassLoop。队友使用其他路径时，只替换项目根路径。

### 7.3 第一次在新机器安装依赖（已有环境可跳过）

不要跨机器复制 `.venv` 或 `node_modules`；安装 Python、Node.js 后重新构建环境。

后端终端（从项目根目录开始，示例使用 Python 3.12）：

```powershell
cd backend
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
if (-not (Test-Path .env)) { Copy-Item .env.example .env }
```

前端另一个终端（从项目根目录开始）：

```powershell
cd frontend
npm ci
if (-not (Test-Path .env)) { Copy-Item .env.example .env }
```

已有 `.env` 不要覆盖；终端显示 `(base)` 不影响显式调用项目 `.venv\Scripts\python.exe`。

### 7.4 日常启动顺序

**A. 需要图谱时，先打开 Docker Desktop，等待引擎运行，然后在 backend 目录执行：**

```powershell
docker info
docker compose -f .\docker-compose.neo4j.yml up -d
docker compose -f .\docker-compose.neo4j.yml ps
```

看到 `classloop-neo4j` 为 Up / running 后仍可能需要等待 Neo4j 初始化；最后以管理员页面“已连接”为准。首次可能需要联网拉取镜像。容器后台运行后，该终端可以关闭。

**B. 后端终端：**

```powershell
cd "C:\Users\86185\Desktop\2026-2027\大数据技术课程实践\ClassLoop\backend"
.\.venv\Scripts\python.exe -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8100
```

**C. 前端另开终端：**

```powershell
cd "C:\Users\86185\Desktop\2026-2027\大数据技术课程实践\ClassLoop\frontend"
npm run dev -- --host 127.0.0.1
```

后端和前端终端必须保留。浏览器打开 `http://127.0.0.1:5173`。如果服务已在运行，不要重复占用端口。

### 7.5 地址、配置与本地演示账号

| 项目 | 默认值 |
|---|---|
| 首页 / 学生 / 教师 / 管理员 | http://127.0.0.1:5173/ 、/student、/teacher、/admin |
| 学生答题 / 结果页 | /input?session=课堂码 、/output?session=课堂码 |
| API 健康 / 接口文档 | http://127.0.0.1:8100/api/health 、http://127.0.0.1:8100/docs |
| 教师 | teacher@classloop.local / classloop123 |
| 管理员 | admin@classloop.local / classloop-admin |
| 预置演示课堂码 | 240805；仅在演示数据未删除/更改时有效，最好从教师工作台获取当前码 |
| Neo4j Browser | http://127.0.0.1:7475 |
| Neo4j Bolt / 账号 | bolt://127.0.0.1:7688；neo4j / classloop-graph |
| 容器映射 | 主机 7475 → 容器 7474；主机 7688 → 容器 7687 |
| 前端配置 | frontend/.env：VITE_API_BASE_URL=http://127.0.0.1:8100 |
| 后端配置 | backend/.env：CLASSLOOP_DATABASE_PATH、CLASSLOOP_NEO4J_URI/USER/PASSWORD/DATABASE 等 |

以上都是本地默认演示账号，不用于公开部署。已有数据库可能已改账号；更改管理员环境变量不会自动覆盖现有账号。已有 Neo4j 数据卷的密码也不一定随 `.env` 修改而重置，不要通过删除数据卷来“修复”密码。

### 7.6 教师课件 UI 尚未完成，怎样导入？

通过后端 API。以下命令是**手动操作说明，不代表资料已经导入**。文件上限 25 MB，仅支持 `.pdf` / `.pptx`；运行前检查文件大小和内容。

```powershell
cd "C:\Users\86185\Desktop\2026-2027\大数据技术课程实践\ClassLoop"
$loginBody = @{ email = 'teacher@classloop.local'; password = 'classloop123' } | ConvertTo-Json
$login = Invoke-RestMethod -Method Post -Uri 'http://127.0.0.1:8100/api/auth/login' -ContentType 'application/json' -Body $loginBody
$headers = @{ Authorization = "Bearer $($login.access_token)" }

# 不带 session_id 时作为教师独立课件导入；绑定课堂需追加 -F "session_id=实际课堂ID"。
# 注意这里是 session_... 的数据库 ID，不是 6 位课堂码。
curl.exe --fail -sS -X POST 'http://127.0.0.1:8100/api/documents/ingest' -H "Authorization: Bearer $($login.access_token)" -F "file=@数据结构课件/5.树和二叉树-2024.pdf"

Invoke-RestMethod -Uri 'http://127.0.0.1:8100/api/documents' -Headers $headers
```

| 接口 | 用途 |
|---|---|
| GET /api/sessions | 使用教师令牌查询自己的课堂及数据库 ID |
| POST /api/documents/ingest | multipart 上传文件，可选 session_id |
| GET /api/documents/{id} | 查询逐页文本、状态 |
| GET /api/documents/{id}/graph | 教师查看 SQLite 保存的图谱 JSON，并非实时查询 Neo4j |
| POST /api/documents/{id}/sync-graph | 启动 Neo4j 后重试同步 |
| DELETE /api/documents/{id} | 删除课件及图谱，属于破坏性操作，演示不要误用 |
| GET /api/admin/overview | 管理员业务汇总 |
| GET /api/admin/documents | 管理员全体教师课件列表 |
| GET /api/admin/graph/status | Neo4j 连通状态 |
| GET /api/admin/documents/{id}/graph | 管理员从 Neo4j 直接查询课件图谱 |

### 7.7 停止、报错与多设备注意事项

| 场景 | 处理方法 |
|---|---|
| 正常停止前后端 | 在对应终端 Ctrl+C；不要直接删除数据文件 |
| 停止图数据库 | backend 目录执行 docker compose -f .\docker-compose.neo4j.yml stop；保留数据卷 |
| 页面打开但登录/课堂查询失败 | 先检查 8100/api/health；核对 VITE_API_BASE_URL；改前端 .env 后重启 Vite |
| Neo4j 未连接 | docker info → compose ps → compose logs --tail 50；核对 7688、账号密码与 .env，修改后重启后端 |
| Docker 管道/权限错误 | 确认 Docker Desktop 引擎已启动；必要时在有权限的 PowerShell 手动执行，不能只安装 Docker 而未启动 |
| 端口占用 | 检查已运行的服务，不要盲目停止其他项目；若更改后端端口，前端配置也要一致 |
| 无教师课件上传按钮 | 这是当前待开发功能，不是启动错误；暂用上述 API |
| 图谱页没看到真实课件 | 放入文件夹不会自动入库，需先提取/同步；确认 graphStatus 和 Neo4j 当前连接 |
| 刷新后学生名字/头像消失 | 符合临时身份设计；刷新可能成为新参与者，原回答仍保存 |
| 换电脑没有原来数据 | 源码不含 Docker 卷；需单独备份/迁移 SQLite 与 Neo4j，或用资料重新导入 |
| 手机扫 localhost 二维码失败 | localhost 指向手机自身。当前默认只支持本机；多设备演示需监听局域网、修改 API 地址、CORS 和防火墙规则后联调 |
| 正式对外访问 | 当前不建议；先修第 9 节权限与文件安全问题，不要直接公网暴露开发服务器或 Neo4j |

## 8. 验证记录与周一前目标

### 8.1 最近实际做过的验证（历史记录，不代表服务一直在线）

| 验证项 | 结果 |
|---|---|
| 前端 npm run lint | 通过；本项目脚本实际执行 TypeScript 检查 + Vite build，不等于完整 ESLint/单元测试覆盖 |
| Python py_compile | main/database/document_graph/security 编译通过；不等于业务测试全部通过 |
| 单选独立性 | 用旧 unique 类型建测试题，保存为普通单选；两名学生选择相同选项均成功；测试课堂已清理 |
| SSE | 曾完成加入、提交、广播等端到端验证；无大班并发压测数据 |
| 文档入库 | 真实 8 页项目 PPT 和临时中文 PDF 测试通过；临时 PDF 已清理 |
| Neo4j 同步幂等性 | 8 页项目 PPT 重复同步，仍为 77 节点/136 关系 |
| 管理员图谱 API | source=neo4j，77 节点/136 关系，8 个页面节点 |
| 管理员浏览器 | 已验证登录、双标签、概览/第 1 页子图；当时控制台无错误 |

### 8.2 周一前的最小可交付目标

| 优先级 | 工作 | 验收标准 | 当前状态 |
|---|---|---|---|
| P0 | 选择真实教学片段 | 5—8 页、一个明确知识点、3 道题；记录原课件来源和页码 | 资料已到位，尚未选页 |
| P0 | 教师课件管理入口 | 网页上传、绑定课堂、展示解析状态、查看逐页文本、失败重试 | API 已有，前端待开发 |
| P0 | 教师页码同步 | 教师点到第 N 页，学生同步显示第 N 页/提取文本；断线后可恢复当前页 | 待开发 |
| P0 | 学生页级反馈 | “不理解/太快/有疑问”连同课堂、课件、页码写入 SQLite；不公开学生身份给同学 | 待开发 |
| P0 | 教师页级提示 | 对应页面显示反馈数/疑问，教师可广播回应；反馈页码不串页 | 待开发 |
| P0 | 清除误导性演示 | 将写死误区和占位按钮明确标注或替换为真实当前题目统计 | 待完善 |
| P0 | 全流程排练/录屏 | 从空课堂开始，两个独立学生页面同时答同选项，演示切页反馈、数据库记录 | 待完成 |
| P1 | 一个真实模型诊断接口 | 输入当前页及脱敏回答，输出误区/证据/建议；超时有明确降级，教师审核 | 待开发，时间允许再做 |
| P1 | 初测/复测关联 | 回答绑定测次，程序算变化，不用预设数字证明效果 | 待开发 |
| 后续 | OCR、全格式课件还原、完整 RAG、大规模集群 | 单独制定指标与验证计划 | 不列为本周必须 |

实施顺序：**真实样例 → 教师上传入口 → 当前页状态/SSE → 页级反馈入库与展示 → 排练 → 视时间接一个模型能力。** 初版页码/文本同步不等于完整 PPT 动画/版式同步，应明确告知老师。任务尚未分派，队友按上述边界认领，避免同时改同一个主文件。

建议演示顺序：项目问题与定位 → 教师建课堂 → 两名学生加入作答 → 教师实时统计/广播 → 真实课件与图谱 → 新完成的页级反馈 → 管理员双库视图 → 明确未完成项。使用模拟学生/预置数据时现场说明；不声称已经过真实大班教学实验。

## 9. 必须知道的技术债与风险

| 项目 | 现状 | 接手注意事项 |
|---|---|---|
| 结果 PIN 安全 | 主要依赖前端门禁；公开回答接口没有完整后端权限检查，公开课堂序列化还包含 PIN 字段 | 对外试用前需隐藏敏感字段、做服务端访问授权 |
| 学生身份安全 | 以客户端临时 participantId 区分参与者，并非强认证 | 需防伪造访问/提交；不要存敏感个人信息 |
| 题目/回答校验 | 已有部分检查，仍需加强题型、选项范围、条件逻辑与禁用题提交验证 | 不能只依靠前端禁用按钮 |
| 文件访问 | 当前有公共文件与题目接口，访问控制不足 | 加权限、大小/类型校验；未完成生产安全审计 |
| 多进程实时 | 在线名单和 SSE 在单进程内存中 | 目前只开一个后端 worker；多 worker 会分裂状态，后续再做共享状态/消息系统 |
| 临时身份取舍 | 保留回答但不保留真实昵称/头像 | 不能同时承诺长期实名追踪与退出即清除身份 |
| 图谱语义质量 | 本地规则关键词、包含/顺序/提及关系 | 未实现概念消歧、先修关系验证、因果推理 |
| 同步任务 | 暂无后台队列，解析/同步由请求触发，失败需手动重试 | 大文件/断线需评估耗时；SQLite 与 Neo4j 不是跨库原子事务 |
| UI 残留 | 部分英文提示、固定课程上下文、占位按钮仍在 | 演示前明确标识；管理员视图也不是完整数据库管理工具 |
| 数据/账号默认值 | 存在演示账号与预置回答 | 外发前处理默认密码和数据，不能将样例当效果证据 |
| 开源归属 | 使用 PollUP 参考；旧文档记有许可证核验待办 | 分发前核验上游许可并保留归属，不自行宣称原创或已有完整许可 |

## 10. 接手代码导航与文档优先级

| 文件/目录 | 修改职责 |
|---|---|
| frontend/src/App.tsx | 三入口路由、教师/管理员入口判断 |
| frontend/src/services/dataProvider.tsx | API 映射、令牌、查询与 SSE；新增后端接口需要同步前端映射 |
| frontend/src/components/ParticipantView.tsx | 学生身份、题目作答、广播展示；页级反馈拟接这里或拆独立组件 |
| frontend/src/components/ResultsView.tsx | 教师统计、参与者、广播、CSV |
| frontend/src/components/SessionDetail.tsx | 课堂详情与题目管理；教师课件入口可从这里接入 |
| frontend/src/components/AdminView.tsx | 双库只读视图及 SVG 图谱 |
| frontend/src/components/MisconceptionPanel.tsx | 当前规则误区演示与占位按钮 |
| backend/app/main.py | 当前主要 API；新增功能尽量逐步拆模块，避免多人同时大改 |
| backend/app/database.py | SQLite 表结构、初始化、演示数据、旧唯一单选迁移 |
| backend/app/security.py | 密码/令牌工具 |
| backend/app/document_graph.py | 文档提取、切块、关键词、Neo4j 同步/读取/删除 |
| backend/docker-compose.neo4j.yml | 独立 Neo4j 容器、端口和持久卷 |
| 数据结构课件/ | 新增课程资料；源文件保持不变 |
| PollUP/、外部 venture_agent | 只读参考/工具边界，不在其中开发 ClassLoop |
| CLASSLOOP_MIGRATION_PLAN.md | 2026-08-31 的历史考察/迁移设计，含旧进度、Convex/WebSocket/唯一选项等过时描述，不作为当前实现依据 |

接手顺序：**本文 → README 启动 → 浏览器走一遍三端 → 按职责阅读源码 → 认领第 8 节目标**。出现文档冲突时，以当前运行代码和可复现测试为准，更新本文的日期和进度，不继续累加互相矛盾的阶段声明。
